-- Create ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- Create a Frame to hold buttons
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 250, 0, 250) -- Set size to 250x250 pixels
frame.Position = UDim2.new(0.25, 0, 0.25, 0)
frame.BackgroundColor3 = Color3.fromRGB(50, 50, 50) -- Dark background
frame.BorderSizePixel = 0 -- Remove border
frame.Parent = screenGui

-- Add a UIStroke to the frame for a border effect
local uiStroke = Instance.new("UIStroke")
uiStroke.Color = Color3.fromRGB(255, 255, 255) -- White border
uiStroke.Thickness = 2 -- Border thickness
uiStroke.Parent = frame

-- Add a UICorner to round the corners of the frame
local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0.1, 0) -- Rounded corners
uiCorner.Parent = frame

-- Function to create a button
local function createButton(name, text, position, action)
    local button = Instance.new("TextButton")
    button.Name = name
    button.Text = text
    button.Size = UDim2.new(0.8, -1, 0.075, 1)
    button.Position = position
    button.BackgroundColor3 = Color3.fromRGB(100, 100, 100) -- Button color
    button.TextColor3 = Color3.fromRGB(255, 255, 255) -- Text color
    button.Font = Enum.Font.SourceSansBold -- Bold font
    button.TextSize = 20 -- Text size
    
    -- Add a UICorner to round the corners of the button
    local buttonUICorner = Instance.new("UICorner")
    buttonUICorner.CornerRadius = UDim.new(0.1, 0) -- Rounded corners
    buttonUICorner.Parent = button
    
    button.Parent = frame
    button.MouseButton1Click:Connect(action)
end

-- Make the frame draggable
local dragToggle = nil
local dragStart = nil
local startPos = nil

local function updateInput(input)
    local delta = input.Position - dragStart
    frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

frame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragToggle = true
        dragStart = input.Position
        startPos = frame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragToggle = false
            end
        end)
    end
end)

frame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        input = input
    end
end)

game:GetService("UserInputService").InputChanged:Connect(function(input)
    if dragToggle then
        updateInput(input)
    end
end)

-- Example buttons
createButton("Button1", "Airbase", UDim2.new(0.1, 0, 0.05, 0), function()
    local targetPosition = Vector3.new(233.48690795898438, 3.4412808418273926, 617.1038208007812)

    -- Teleport the player's character to the target position
    local function teleportPlayer()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        
        character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(targetPosition)
    end

    -- Teleport player on command
    teleportPlayer()
end)

createButton("Button2", "Guard Armory", UDim2.new(0.1, 0, 0.15, 0), function()
    local targetPosition = Vector3.new(79.27246856689453, 2.999999761581421, -69.04203033447266)

    -- Teleport the player's character to the target position
    local function teleportPlayer()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        
        character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(targetPosition)
    end

    -- Teleport player on command
    teleportPlayer()
end)

createButton("Button3", "Guard Spawn", UDim2.new(0.1, 0, 0.25, 0), function()
    local targetPosition = Vector3.new(63.02465057373047, 2.999999761581421, -61.568904876708984)

    -- Teleport the player's character to the target position
    local function teleportPlayer()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        
        character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(targetPosition)
    end

    -- Teleport player on command
    teleportPlayer()
end)

createButton("Button4", "Vehicle Spawn", UDim2.new(0.1, 0, 0.35, 0), function()
    local targetPosition = Vector3.new(1480.160400390625, 3.999999761581421, 74.6236343383789)

    -- Teleport the player's character to the target position
    local function teleportPlayer()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        
        character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(targetPosition)
    end

    -- Teleport player on command
    teleportPlayer()
end)

createButton("Button5", "Clothing Store", UDim2.new(0.1, 0, 0.45, 0), function()
    local targetPosition = Vector3.new(1767.302734375, 4.100001335144043, 119.07331085205078)

    -- Teleport the player's character to the target position
    local function teleportPlayer()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        
        character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(targetPosition)
    end

    -- Teleport player on command
    teleportPlayer()
end)

createButton("Button6", "Theatre", UDim2.new(0.1, 0, 0.55, 0), function()
    local targetPosition = Vector3.new(1944.4127197265625, 3.999999761581421, 70.38367462158203)

    -- Teleport the player's character to the target position
    local function teleportPlayer()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        
        character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(targetPosition)
    end

    -- Teleport player on command
    teleportPlayer()
end)

createButton("Button7", "Safe House", UDim2.new(0.1, 0, 0.65, 0), function()
    local targetPosition = Vector3.new(2487.84326171875, 3.5003795623779297, 19.08624839782715)

    -- Teleport the player's character to the target position
    local function teleportPlayer()
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        
        character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(targetPosition)
    end

    -- Teleport player on command
    teleportPlayer()
end)

createButton("Button8", "Vehicle Shop (Job 1)", UDim2.new(0.1, 0, 0.75, 0), function()
local targetPosition = Vector3.new(1810.1351318359375, 3.499999761581421, 80.68644714355469)

-- Function to teleport the player's character to the target position
local function teleportPlayer()
    local player = game.Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    
    character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(targetPosition)
end

-- Teleport player on command
teleportPlayer()
end)

createButton("Button8", "Book Writing (Job 2)", UDim2.new(0.1, 0, 0.85, 0), function()
local targetPosition = Vector3.new(1727.126220703125, 3.999999761581421, 240.60963439941406)

-- Function to teleport the player's character to the target position
local function teleportPlayer()
    local player = game.Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    
    character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(targetPosition)
end

-- Teleport player on command
teleportPlayer()

end)

createButton("Button9", "Infinite Yield", UDim2.new(0.1, 0, 0.95, 0), function()
loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()
end)
